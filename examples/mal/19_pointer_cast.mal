 ; Pointer casting operations
 function @pointer_ops(ptr %p, i64 %offset) -> ptr {
 entry:
   %int_val = ptrtoint ptr %p to i64
   %adjusted = add i64 %int_val, %offset
   %new_ptr = inttoptr i64 %adjusted to ptr
   ret ptr %new_ptr
 }
